var React = require('react');

var Bar = React.createClass({

    render: function() {
        return (
            <div>
                Hello bar.
            </div>
        );
    }

});

module.exports = Bar;
